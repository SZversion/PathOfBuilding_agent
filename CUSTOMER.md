# PoB Agent 고객 정의

## 대상 사용자

주요 사용자는 Path of Exile을 알고 있지만 PoB의 세부 UI와 계산 구조에는 익숙하지 않은 일반 플레이어다. 사용자는 현재 빌드가 왜 그런 수치를 내는지 알고 싶고, 복잡한 PoB 조작 없이 자연어로 아이템·젬·패시브 변경을 요청하고 싶어 한다.

PoE와 PoB를 모두 잘 아는 사용자는 상세 trace와 Evidence Graph를 검증용으로 사용할 수 있지만, 기본 답변은 PoB를 처음 깊게 다루는 사용자도 이해할 수 있어야 한다.

## MVP에서 해결하는 일

1. 수치 조회와 근거 추적
2. 아이템 교체·비교
3. 젬 교체·비교
4. 패시브·스킬 군 주얼 변경·비교
5. 변경 전·후 DPS와 메커니즘 설명
6. 현재 빌드 메커니즘 설명
7. PoB 코드로 불러온 다른 빌드와 비교·차이 설명

AI가 사용자의 목표에 맞는 아이템·젬·패시브를 자동 추천하는 기능은 MVP에 포함하지 않는다.

## 사용 흐름

1. 사용자는 기존 PoB를 사용하듯 빌드를 열거나 PoB 코드를 붙여넣는다.
2. PoB의 작은 AI 버튼을 눌러 별도 Agent 창을 연다.
3. Agent는 현재 Skill Set, Main Skill, Config, 활성 상태를 기준으로 질문을 처리한다.
4. 사용자는 “왜 Arc의 시전 속도가 이 값인가?”, “이 무기로 바꾸면 DPS가 얼마나 오르는가?”, “이 빌드와 무엇이 다른가?”처럼 자연어로 질문한다.
5. Agent는 필요하면 여러 Tool을 순서대로 호출하고, 변경 요청은 현재 PoB 화면에 즉시 반영한다.
6. 사용자는 기존 PoB의 `Save` 또는 `Save As`를 직접 눌러 저장 여부와 파일 분리를 결정한다.

## 입력

- 한국어 또는 영어 자연어 질문
- 한국어·영어 아이템/스킬명
- 게임 내 경매장 복사 아이템 텍스트
- PoB 코드
- 현재 PoB에서 선택된 Skill Set, Main Skill, Config, 활성 버프·조건
- “메인 스킬”, “주력기”, “6링크”처럼 문맥상 생략된 표현

대상이 여러 개로 해석되거나 필수 정보가 빠진 경우 Agent는 임의로 선택하지 않고 짧은 추가 질문을 한다.

## 기본 출력 경험

기본 답변은 한국어로 친절하게 제공하며, 다음 순서를 따른다.

- 결론과 최종 수치
- 적용된 효과와 상호작용
- 상세 계산 과정과 중간값
- Evidence Graph
- PoB 내부 출처, 로컬 지식, 외부 출처
- 확실성 수준과 버전 정보

숫자는 PoB 계산값을 원본 정밀도로 보존하고 표시 형식에서만 반올림한다. PoB에서 계산된 값, 외부 문서의 참고값, 모델의 추론을 서로 다른 표식으로 보여준다.

## 변경·저장 경험

사용자가 특정 아이템·젬·패시브·스킬 군 주얼의 교체를 요청하면 Agent는 현재 화면의 메모리 상태에 즉시 적용한다. 현재 파일을 자동 저장하거나 원본을 자동 복사하지 않는다. 저장은 PoB의 기존 `Save`와 `Save As`를 그대로 사용한다.

`Save As`가 성공하면 기존 빌드의 로컬 Agent 캐시를 새 빌드 캐시로 복사하고, 이후 두 캐시는 독립적으로 관리한다. 대화와 Evidence Graph 참조는 빌드별 사용자 PC 캐시에 남는다. 외부 AI 서버에는 영구 저장되지 않는다.

## 외부 AI와 데이터 안내

MVP는 사용자 API 키나 로컬 모델 설치를 요구하지 않는다. 질문의 자연어 해석은 외부 AI 서버에서 처리되지만, 외부 AI가 로컬 파일·검색·Tool에 직접 접근하지는 않는다. 외부 AI가 반환한 구조화 계획을 바탕으로 로컬 PoB Bridge가 PoB 상태 접근·계산·변경·재계산, 로컬 RAG, 허용 외부 검색을 수행하고 결과를 다시 AI에 전달한다. 외부로는 원본 XML이 아니라 정규화된 빌드 snapshot을 보낸다.

- 첫 요청: 전체 snapshot
- 이후 요청: 이전 상태와의 변경분
- 변경 감지가 불확실한 경우: 전체 최신 snapshot으로 재동기화
- 게임 캐릭터와 빌드 정보는 서비스 동작을 위해 전송될 수 있음
- 사용자의 직접 PoB 변경은 Agent 변경보다 우선함
- Agent 변경은 화면에 즉시 적용되지만 파일에는 자동 저장되지 않으며, 사용자가 PoB의 `Save` 또는 `Save As`를 눌러야 저장됨
- 사용자 API 키는 모델 입력·snapshot·로그·캐시에 포함되지 않음

## 실패·거부 경험

- 정보가 부족하면 필요한 정보를 질문한다.
- 게임/PoB 메이저 버전이 다르면 계산과 비교를 거부한다.
- 세부 버전이 다르면 업데이트를 시도하고, 실패해도 메이저 버전이 같으면 경고와 함께 기존 캐시를 사용할 수 있다.
- PoB·로컬 지식·허용된 외부 출처에서 근거를 찾지 못하면 추측하지 않고 답변을 거부한다.
- 외부 검색값은 “PoB 계산값이 아님”과 출처 URL·문서명·확인 시점을 표시한다.
- 모델 추론은 확정값이 아니라 “추론” 또는 “확인되지 않은 설명”으로 명시한다.
- Agent 작업 중 사용자가 PoB UI를 변경하면 사용자 상태를 보존하고 오래된 Agent 변경을 폐기한다.
- Tool 한 요청의 최대 연속 호출은 8회다. 한도를 넘기면 중단 이유와 확보된 부분 결과를 표시한다.

## 성공 경험

PoE는 알지만 PoB가 익숙하지 않은 사용자가 별도의 XML 편집이나 복잡한 계산식 분석 없이, 자연어 질문만으로 현재 빌드의 수치·상호작용·변경 전후 차이와 모든 근거를 이해할 수 있어야 한다.
